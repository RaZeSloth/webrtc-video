{
  "name": "webrtc-video-loovtoo",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.17.1",
    "serve": "^13.0.2",
    "socket.io": "^4.4.1"
  },
  "scripts": {
    "start": "node server.js",
    "start:client": "cd ./client && serve -s build -p $(echo $PORT)",
    "postinstall": "cd ./client && yarn && yarn build"
  },
  "engines": {
    "node": "17.x"
  }
}
